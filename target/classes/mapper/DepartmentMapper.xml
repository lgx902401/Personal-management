<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nbclass.mapper.DepartmentMapper">
    <resultMap id="BaseResultMap" type="com.nbclass.model.Department">
        <!--
          WARNING - @mbggenerated
        -->
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="department_id" property="departmentId" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="parent_id" property="parentId" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
        -->
        id, department_id, name, description, status, parent_id, create_time, update_time
    </sql>
    <select id="findByUserId" parameterType="string" resultMap="BaseResultMap">
        SELECT d.department_id
        FROM department d LEFT JOIN user_department ud on d.department_id = ud.department_id
        WHERE ud.user_id = #{userId,jdbcType=VARCHAR}
    </select>
    <select id="selectAllDepartment" parameterType="integer" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM department WHERE status=#{status}
    </select>
    <select id="selectByDepartmentId" parameterType="string" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM department
        WHERE department_id = #{departmentId}
    </select>
    <select id="selectDepartments" parameterType="com.nbclass.model.Department" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        FROM department
        WHERE status=1
        <if test="departmentId !=null and departmentId != ''">
            AND department_id like concat('%', #{departmentId}, '%')
        </if>
        <if test="name !=null and name != ''">
            AND name like concat('%', #{name}, '%')
        </if>
        <if test="description !=null and description != ''">
            AND description like concat('%', #{description}, '%')
        </if>

    </select>

    <update id="updateStatusBatch" parameterType="map">
        UPDATE
        department
        SET
        status=#{status}, update_time=now()
        where
        department_id in
        <foreach collection="departmentIds" item="item" index="index" open="(" separator="," close=")">
            #{item}
        </foreach>
    </update>

    <update id="updateByDepartmentId" parameterType="map">
     UPDATE
      department
      SET
      name=#{name},description=#{description}, parent_id=#{parentId},update_time=now()
      where
      department_id = #{departmentId}
  </update>
    <delete id="deleteByDepartmentId" parameterType="com.nbclass.model.Department">
        delete from department where department_id =#{departmentId}
    </delete>

</mapper>